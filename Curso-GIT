Curso GIT

# Verificar versão
git --version

#Git config.
user.name = marxviana
user.email - marxviana@gmail.com
init.defaultBranch

git config --global <COMANDO>
EX: git config --global init.defaultBranch <name>


# Iniciar o repositorio é mostrar para o git onde será seu repositório


A pasta .git será a pasta de versionamento (Rastreamento)

#Esse comando leva o arquivo para um estado de "staged", ou seja, pronto para realizar o commited
	git add 

# Para retirar do estado "staged" para "unstage"
git rm --cached <nomeArquivo>

# Para adicionar todos os arquivos para estarem pronto para realizar o commited, ou seja, coloca-los em um estado de "staged"
	git add --all OR git add .

#Commit irá pegar todos os arquivos em um estado de "staged" e irá fazer um SNAPSHOT desses arquivos
	git commit -m"mensagem do que esse commit está adicionando"

# Verificar o que foi alterado
git diff


#Voltar para a versão anterior
git restore


# para ver o log
git log

# para ver o log por linha e numero
git log --online

#Fazer o commit sem passar pelo "estado de staging"
git commit -a -m "comentario"

#Para recuperar arquivos que foram delatados e estão em staged
git restore --staged<nome do arquivo>

#Para recuperar arquivos que foram delatados
git restore <nome do arquivo>







